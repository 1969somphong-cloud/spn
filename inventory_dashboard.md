# Inventory Management Dashboard

## วิธีสร้างตาราง Excel ที่ใช้งานได้

ตามข้อมูลจากไฟล์ CSV ของคุณ ฉันได้สร้างคำแนะนำในการสร้างตาราง Excel ที่สามารถใช้งานได้ดังนี้:

### โครงสร้างตาราง

#### ส่วนที่ 1: ตารางข้อมูลสินค้า (Sheet: Data)

| รายการ (Column A) | ชื่อสินค้า (Column B) | จำนวนสต็อก (Column C) |
|---|---|---|
| 1 | [ชื่อสินค้า 1] | [จำนวน] |
| 2 | [ชื่อสินค้า 2] | [จำนวน] |
| 3 | [ชื่อสินค้า 3] | [จำนวน] |
| ... | ... | ... |
| 100 | [ชื่อสินค้า N] | [จำนวน] |

#### ส่วนที่ 2: ตาราง Summary (Sheet: Dashboard หรือ Summary)

| รายการ | สูตร | ค่า |
|---|---|---|
| จำนวน SKU ทั้งหมด | `=COUNTA(Data!A2:A100)` | [ผลลัพธ์อัตโนมัติ] |
| สต็อกรวมทั้งหมด | `=SUM(Data!C2:C100)` | [ผลลัพธ์อัตโนมัติ] |
| สินค้าที่มีสต็อกมากที่สุด | `=INDEX(Data!B2:B100, MATCH(MAX(Data!C2:C100), Data!C2:C100, 0))` | [ผลลัพธ์อัตโนมัติ] |
| จำนวนสูงสุด | `=MAX(Data!C2:C100)` | [ผลลัพธ์อัตโนมัติ] |
| สินค้าที่มีสต็อกน้อยที่สุด | `=INDEX(Data!B2:B100, MATCH(MIN(Data!C2:C100), Data!C2:C100, 0))` | [ผลลัพธ์อัตโนมัติ] |
| จำนวนต่ำสุด | `=MIN(Data!C2:C100)` | [ผลลัพธ์อัตโนมัติ] |
| จำนวนสินค้าสต็อกติดลบ | `=COUNTIF(Data!C2:C100,"<0")` | [ผลลัพธ์อัตโนมัติ] |

### ขั้นตอนสร้างไฟล์ Excel:

1. **สร้าง Sheet แรก ชื่อ "Data"**
   - Cell A1: "รายการ"
   - Cell B1: "ชื่อสินค้า"
   - Cell C1: "จำนวนสต็อก"
   - ป้อนข้อมูลสินค้าในแถวที่ 2-100

2. **สร้าง Sheet ที่สอง ชื่อ "Dashboard"**
   - ใส่ตารางสูตรตามที่แสดงด้านบน
   - สูตรจะอ้างอิงไปยัง Sheet "Data"

3. **จัดรูปแบบตาราง**
   - ใช้ตัวหนา (Bold) สำหรับหัวตาราง
   - ใช้สีพื้นหลังเพื่อให้เห็นชัดเจน
   - ปรับความกว้างของคอลัมน์ให้เหมาะสม

### หมายเหตุ:
- สูตรจะอัปเดตอัตโนมัติเมื่อคุณเปลี่ยนแปลงข้อมูลในแถว 2-100
- หากข้อมูลเกินกว่า 100 แถว ให้ขยายช่วงในสูตร (เช่น A2:A200)
- ตรวจสอบให้แน่ใจว่าข้อมูลสต็อกมีรูปแบบตัวเลข
